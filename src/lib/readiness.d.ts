export function showsSetupSplash(state: { setupChecked: boolean; hasHousehold: boolean }): boolean
export function showsUnreachable(state: {
  setupChecked: boolean
  hasHousehold: boolean
  setupFailed: boolean
}): boolean
